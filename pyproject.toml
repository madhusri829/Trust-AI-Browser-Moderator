[project.scripts]
server = "server.app:main"

[project]
name = "trust-ai-moderator"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0",
    "requests"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build_meta"
